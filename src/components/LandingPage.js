import React from 'react';

const LandingPage = () => (
    <div id="landing_page">
        <header>
            <h1>This is the landing page</h1>
        </header>
    </div>
);

export default LandingPage;